/**
 * Products listing and details layout
 * Provides consistent styling for product browsing pages
 */

export default function ProductsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
